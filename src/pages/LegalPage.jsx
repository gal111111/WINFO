import { getUi } from '../content/site';
import { Breadcrumb, Checklist, SectionHeading } from '../components/Sections';
export function LegalPage({ navigate, kind, language }) { const copy = getUi(language); const isPrivacy = kind === 'privacy'; const paragraphs = isPrivacy ? copy.legalPrivacy : copy.legalTerms; return <section className="section legal-page"><div className="container"><Breadcrumb current={isPrivacy ? copy.privacyTitle : copy.termsTitle} navigate={navigate} language={language} /><div className="legal-heading"><SectionHeading label={isPrivacy ? copy.privacyTitle : copy.termsTitle} title={isPrivacy ? copy.privacyTitle : copy.termsTitle} text={copy.legalUpdated} /></div><div className="legal-copy"><Checklist dark items={paragraphs} /></div></div></section>; }
